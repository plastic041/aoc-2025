{
    "name": "rust-devcontainer",
    "image": "mcr.microsoft.com/devcontainers/rust:latest",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libz3-dev libclang-dev && rustc --version",
    "remoteUser": "vscode"
}
